import React from 'react';
import { Link } from "gatsby";
import Layout from '../components/Layout';

export default () => {
    return (
        <div>
            <Layout> 
                <p>Trying to develops internet scale global apps using JAMstack and multi-cloud </p>
            </Layout>
        </div>
    );
}
